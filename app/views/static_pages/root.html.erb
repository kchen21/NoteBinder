<script type="text/javascript">
  <% if signed_in? %>
    window.currentUser = <%=
    render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe
    %>
  <% end %>
  window.assets = {
    logo: "<%= image_url('logo.png') %>",
    logo_wordless: "<%= image_url('logo_wordless.png') %>"
  }
</script>

<main id="root"></main>
